export const styles = {
    normalPlayer : {
        display: "flex",
        flexDirection: "column", 
        alignItems:"center", 
        justifyContent:"center"
    },
    clickedPlayer : {
        display: "flex",
        flexDirection: "column", 
        alignItems:"center", 
        justifyContent:"center",
        border: '1px solid #ccc',
        boxShadow: '0 0 10px rgba(0, 0, 0, 0.3)',
        borderRadius: '10px'
    }
}